- Install pdf-reader in vscode.
- In tom_session_data.txt, the headline says "Time since meal", while the values shows the absolute time of the meal before.
- Fell asleep seems to allways include some point at about 4 o'clock, even though there is no such value in tom_session_data. Does this mean that fell asleep does not listen to what is in tom_session_data at all?
    - Total session duration also has odd values and does not listen to the change in tom_session_data either. Does that mean nobody cares about tom_session_data?
    - Duration of session also has an odd value, and does not care about tom_session_data
- Something is wrong with total duration of deep work sessions vs. dates
- Change axis of when he fell asleep, so it does not show 25 o'clock
- Make shorter plot for duration of deep work sessions!!
- Make data_to_file() shorter
- put the struct-function into the function file???
- Make plot of how often the same activity, the same interest-level during deep work session
- Make shorter plots of
    - dates
    - fell asleep
    - woke up
    - time awake
    - hours of sleep
    - sleep quality
    - nr of sessions
    - total length of sessions
    to fit the times he had deep work sessions
- Find average of the different things when he had a deep work session vs. when he did not have a deep work session
- Readme
    - Improve english by dictionary
    - Improve english grammar and flow
    - The data is random, right?
    - Improve installation instructions?
- Replace tom_session_data with tom-session-data